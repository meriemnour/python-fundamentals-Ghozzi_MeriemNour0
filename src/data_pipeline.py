# users, comments, files, books

# (personal data) get users from file -> fill the defaults -> put into database ->
# load from database -> add a comment -> load to mongodb
# books -> embed text content (for search) -> put metadata to relational database,
# put text into (other database) -> load to mongo -> find concepts
# scientific articles
# images
# (numerical data) stocks market
